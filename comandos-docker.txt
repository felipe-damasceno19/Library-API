#Checar se a port esta disponivel
netstat -aof | findstr porta

#Antes de criar e rodar os container do postgres e do pgadmin, é necessário criar uma network
docker network create library-network

# Rodar container Postgre na versão 16.4
docker run --name librarydb -p 5432:5432 -e POSTGRES_PASSWORD=postgres -e POSTGRES_USER=postgres -doce POSTGRES_DB=library --network library-network -d postgres:16.3

# Rodar Pgadmin 4
docker run --name pgadmin4 -p 15432:80 -e PGADMIN_DEFAUPGLT_EMAIL=admin@admin.com -e PGADMIN_DEFAULT_PASSWORD=admin --network library-network -d dpage/pgadmin4:8.9